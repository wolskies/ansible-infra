---
- name: Verify minimal configuration test results
  hosts: all
  gather_facts: true
  tasks:
    - name: Verify roles completed without errors
      ansible.builtin.debug:
        msg: "SUCCESS: All roles handled minimal infrastructure config safely"

---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: ubuntu-flatpak-test
    image: geerlingguy/docker-ubuntu2404-ansible:latest
    pre_build_image: true
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    command: "/lib/systemd/systemd"
    groups:
      - flatpak_hosts
  - name: debian-flatpak-test
    image: geerlingguy/docker-debian12-ansible:latest
    pre_build_image: true
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    command: "/lib/systemd/systemd"
    groups:
      - flatpak_hosts
provisioner:
  name: ansible
  inventory:
    group_vars:
      all:
        # Test flatpak management using new infrastructure structure
        infrastructure:
          Ubuntu:
            flatpak:
              enabled: true
              packages:
                install:
                  - org.freedesktop.Platform//22.08
                remove: []
              flathub: true
              plugins:
                gnome: false
                plasma: false
          Debian:
            flatpak:
              enabled: true
              packages:
                install:
                  - org.freedesktop.Platform//22.08
                remove: []
              flathub: true
              plugins:
                gnome: false
                plasma: false
    host_vars:
      ubuntu-flatpak-test:
        # Host-specific flatpak configuration
        infrastructure:
          Ubuntu:
            flatpak:
              packages:
                install:
                  - org.freedesktop.Platform//22.08
                  - org.freedesktop.Sdk//22.08  # Test additional package
      debian-flatpak-test:
        # Host-specific flatpak configuration  
        infrastructure:
          Debian:
            flatpak:
              packages:
                install:
                  - org.freedesktop.Platform//22.08
                  - org.freedesktop.Sdk//22.08  # Test additional package
  config_options:
    defaults:
      host_key_checking: false
      stdout_callback: yaml
verifier:
  name: ansible

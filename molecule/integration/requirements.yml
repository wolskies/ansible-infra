---
# Collection dependencies for testing
collections:
  - name: community.general
    version: ">=7.0.0"
  - name: community.docker
    version: ">=3.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: devsec.hardening
    version: ">=8.0.0"

# Role dependencies (if any external roles needed for testing)
roles: []

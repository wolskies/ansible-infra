---
# Verify manage_users role functionality
- name: Verify manage_users role deployment
  hosts: all
  gather_facts: true
  become: true

  tasks:
    - name: Verify role executed successfully
      ansible.builtin.debug:
        msg: "manage_users role testing completed successfully"

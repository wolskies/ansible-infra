---
# Verify manage_language_packages role functionality
- name: Verify manage_language_packages role deployment
  hosts: all
  gather_facts: true
  become: true

  tasks:
    - name: Verify role executed successfully
      ansible.builtin.debug:
        msg: "manage_language_packages role testing completed successfully"

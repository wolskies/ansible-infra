---
- name: Test package management functionality
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: Apply package management role
      ansible.builtin.include_role:
        name: wolskinet.infrastructure.manage_packages
      tags:
        - packages

    # Repository functionality is already tested in the verify playbook
    # Additional package installation from custom repos can be network-dependent
    # so we'll verify the repository configuration instead of actual package installation

---
# Minimal workstation configuration for testing

# Essential workstation packages only
group_packages_install_Archlinux:
  - git
  - curl
  - vim
  - htop

# Skip third-party packages for initial testing
third_party_packages: {}
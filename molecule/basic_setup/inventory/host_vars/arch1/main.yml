---
# Host-specific configuration for arch1 workstation
# Minimal workstation configuration for testing

# Workstation-specific settings
workstation_type: development
install_dotfiles: false  # Skip for initial testing

# Security settings (disabled for testing)
security:
  ssh_hardening: false  # Disabled for testing
  firewall_strict: false
  fail2ban: false

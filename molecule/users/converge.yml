---
- name: Converge
  hosts: all
  become: true
  tasks:
    - name: Include os_configuration role for user management
      ansible.builtin.include_role:
        name: wolskies.infrastructure.os_configuration
      tags:
        - users

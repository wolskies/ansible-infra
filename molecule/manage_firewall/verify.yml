---
# Verify manage_firewall role functionality
- name: Verify manage_firewall role deployment
  hosts: all
  gather_facts: true
  become: true

  tasks:
    - name: Verify role executed successfully
      ansible.builtin.debug:
        msg: "manage_firewall role testing completed successfully"

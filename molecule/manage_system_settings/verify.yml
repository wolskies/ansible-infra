---
# Verify manage_system_settings role functionality
- name: Verify manage_system_settings role deployment
  hosts: all
  gather_facts: true
  become: true

  tasks:
    - name: Verify role executed successfully
      ansible.builtin.debug:
        msg: "manage_system_settings role testing completed successfully"

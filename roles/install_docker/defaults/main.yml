---
# Docker installation defaults - passed to geerlingguy.docker
docker_edition: "ce"
docker_install_compose_plugin: true
docker_install_compose: false # Use plugin instead of standalone
docker_users: ["{{ ansible_user | default('') }}"] # Add ansible_user to docker group
docker_daemon_options:
  storage-driver: "overlay2"
  log-driver: "json-file"
  log-opts:
    max-size: "100m"
    max-file: "3"

---
- name: Test go role
  hosts: all
  become: true
  tasks:
    # First create the user for go installation
    - name: Include os_configuration role for user creation
      ansible.builtin.include_role:
        name: wolskies.infrastructure.os_configuration
      tags:
        - users

    # Test Go role
    - name: Test go role
      ansible.builtin.include_role:
        name: wolskies.infrastructure.go

---
# =============================================================================
# NEOVIM ROLE DEFAULTS
# =============================================================================
# This role handles Neovim installation and configuration for individual users
# Uses role-specific variables passed from calling roles
#
# All variables are documented in meta/argument_specs.yml
# =============================================================================

# Configuration Options
neovim_config_enabled: true

---
# =============================================================================
# DOCKER_COMPOSE_GENERIC ROLE - Template role for Docker service deployment
# =============================================================================
# This role uses collection-wide defaults from defaults/main.yml
# Only role-specific overrides and documentation are included here
#
# For complete variable reference, see: defaults/main.yml
# =============================================================================

# All docker service variables are defined in collection defaults
# No role-specific overrides needed

---
# Simplified package discovery using built-in package_facts

- name: Get package facts
  ansible.builtin.package_facts:
    manager: auto

- name: Set host packages install variable (manage_packages role format)
  ansible.builtin.set_fact:
    "host_packages_install_{{ ansible_distribution }}": "{{ ansible_facts.packages.keys() | list | sort }}"

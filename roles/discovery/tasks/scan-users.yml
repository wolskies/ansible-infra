---
# Simplified user discovery - trust Ansible built-ins

- name: Get user and group facts
  ansible.builtin.getent:
    database: "{{ item }}"
  become: true
  loop:
    - passwd
    - group

- name: Set users_config variable (manage_users role format)
  ansible.builtin.set_fact:
    users_config: []

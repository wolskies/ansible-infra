---
# Security Configuration Discovery - Simplified
# Only discovers what's needed by system_security role

- name: Discover security services
  ansible.builtin.set_fact:
    discovered_security:
      ssh:
        service_running: "{{ 'ssh' in ansible_facts.services or 'sshd' in ansible_facts.services }}"
  tags:
    - security

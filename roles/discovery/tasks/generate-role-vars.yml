---
# Generate role-specific variables based on discovery results
# Keep it simple - just ensure discovery_host_vars is structured properly

- name: Ensure discovery_host_vars structure is ready for output
  ansible.builtin.set_fact:
    discovery_host_vars: "{{ discovery_host_vars | default({}) }}"
  tags:
    - discovery
    - role-integration

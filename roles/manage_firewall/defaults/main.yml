---
# =============================================================================
# FIREWALL RULE MANAGEMENT VARIABLES
# =============================================================================

# Example:
# firewall_rules:
#   - rule: allow
#     port: 80
#     proto: tcp
#     comment: "HTTP traffic"
#   - rule: limit
#     port: 22
#     proto: tcp
#     comment: "SSH rate limiting"
firewall_rules: []

firewall_backend: "ufw"

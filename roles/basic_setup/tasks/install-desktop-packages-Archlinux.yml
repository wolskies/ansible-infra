---
# Install desktop packages for Arch Linux systems
# These are GUI applications and desktop environment packages

- name: Install desktop packages via pacman
  ansible.builtin.package:
    name: "{{ final_desktop_packages }}"
    state: present
  become: true
  when: final_desktop_packages | length > 0
  tags:
    - packages
    - desktop-packages
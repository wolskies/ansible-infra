---
# Install development packages for Arch Linux systems
# These are development tools, compilers, etc.

- name: Install development packages via pacman
  ansible.builtin.package:
    name: "{{ final_development_packages }}"
    state: present
  become: true
  when: final_development_packages | length > 0
  tags:
    - packages
    - development-packages
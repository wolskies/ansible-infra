---
# Install media packages for macOS systems
# These are typically large packages for media creation/editing

- name: Install media packages via Homebrew
  community.general.homebrew:
    name: "{{ final_media_packages }}"
    state: present
  become: false
  when: final_media_packages | length > 0
  tags:
    - packages
    - media-packages
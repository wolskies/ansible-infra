---
# Install media packages for Debian family systems
# These are typically large packages for media creation/editing

- name: Install media packages
  ansible.builtin.package:
    name: "{{ final_media_packages }}"
    state: present
  become: true
  when: final_media_packages | length > 0
  tags:
    - packages
    - media-packages
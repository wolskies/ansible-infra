---
# =============================================================================
# USER MANAGEMENT ROLE VARIABLES - System-level account management
# =============================================================================
# Creates/removes user accounts, groups, SSH keys (requires sudo)
# User preferences are configured separately by configure_user role
# =============================================================================

users: []
users_absent: []

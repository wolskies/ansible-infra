dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: ubuntu-flatpak-system-full
    image: geerlingguy/docker-ubuntu2404-ansible:latest
    pre_build_image: true
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    command: /lib/systemd/systemd
    tmpfs:
      - /tmp
      - /run
  - name: arch-flatpak-system-full
    image: carlodepieri/docker-archlinux-ansible:latest
    pre_build_image: true
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    command: /usr/lib/systemd/systemd
    env:
      container: docker
    capabilities:
      - SYS_ADMIN
    ulimits:
      - nofile:65535:65535
    tmpfs:
      - /tmp
      - /run
  - name: ubuntu-flatpak-packages-basic
    image: geerlingguy/docker-ubuntu2404-ansible:latest
    pre_build_image: true
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    command: /lib/systemd/systemd
    tmpfs:
      - /tmp
      - /run
  - name: ubuntu-flatpak-no-action
    image: geerlingguy/docker-ubuntu2404-ansible:latest
    pre_build_image: true
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    command: /lib/systemd/systemd
    tmpfs:
      - /tmp
      - /run
provisioner:
  name: ansible
  config_options:
    defaults:
      remote_tmp: /tmp/ansible-remote-tmp
      local_tmp: /tmp/ansible-local-tmp
      host_key_checking: false
      stdout_callback: ansible.builtin.default
      stdout_callback_format: yaml
  inventory:
    host_vars:
      ubuntu-flatpak-system-full:
        ansible_user: root
        molecule_test: true
        flatpak:
          enabled: true
          flathub: true
          method: system
          plugins:
            gnome: true
            plasma: false
        flatpak_packages:
          - name: org.freedesktop.Platform//23.08
            state: present
          - name: org.mozilla.firefox
            state: present
      arch-flatpak-system-full:
        ansible_user: root
        molecule_test: true
        flatpak:
          enabled: true
          flathub: true
          method: system
          plugins:
            gnome: true
            plasma: true
        flatpak_packages:
          - name: org.freedesktop.Platform//23.08
            state: present
      ubuntu-flatpak-packages-basic:
        ansible_user: root
        molecule_test: true
        flatpak:
          enabled: true
          flathub: true
          method: system
          plugins:
            gnome: false
            plasma: false
        flatpak_packages:
          - name: org.freedesktop.Platform//23.08
            state: present
          - name: unwanted-package
            state: absent
      ubuntu-flatpak-no-action:
        ansible_user: root
        molecule_test: true
        flatpak:
          enabled: false
        flatpak_packages: []
verifier:
  name: ansible

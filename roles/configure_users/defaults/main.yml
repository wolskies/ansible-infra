---
# =============================================================================
# CONFIGURE USER ROLE DEFAULTS
# =============================================================================
# This role configures user preferences (development tools, dotfiles, etc.)
# NOTE: Users must already exist - use ansible.builtin.user to create them first
# =============================================================================

# User Preferences (Collection-wide)
users: []
# Example:
# users:
#   - name: developer  # MUST already exist on target system
#     git:
#       user_name: "John Doe"
#       user_email: "john@example.com"
#       editor: "nvim"
#     nodejs:
#       packages: ["typescript", "@angular/cli"]
#     rust:
#       packages: ["ripgrep", "fd-find"]
#     go:
#       packages: ["github.com/user/package@latest"]
#     neovim:
#       enabled: true
#     terminal_entries: [kitty, alacritty]
#     dotfiles:
#       enable: true
#       repository: "https://github.com/user/dotfiles.git"
#       dest: ".dotfiles"
#       branch: "main"

# Role Operation
# This role processes the collection-wide 'users' variable (list of user objects)
# Each user object configures preferences for an EXISTING user
# User creation, password management, SSH keys, and sudo access should be
# handled separately using ansible.builtin.user and related modules

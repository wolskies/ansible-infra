---
- name: Configure user preferences across all test scenarios
  hosts: all
  become: true
  gather_facts: true
  tasks:
    # REQ-CU-007-012,019: Test configure_users role
    - name: Configure user preferences with role
      ansible.builtin.include_role:
        name: wolskies.infrastructure.configure_users

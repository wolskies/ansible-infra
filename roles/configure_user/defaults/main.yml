---
# =============================================================================
# CONFIGURE_USER ROLE - Per-user preference configuration
# =============================================================================
# This role uses collection-wide defaults from defaults/main.yml
# Configures preferences for users as defined in the users array
#
# For complete variable reference, see: defaults/main.yml
# =============================================================================

# This role configures preferences for individual users
# Users are defined in the main users array with all preferences included

---
# =============================================================================
# CONFIGURE USER ROLE DEFAULTS
# =============================================================================
# This role configures a single user and their preferences
# Uses collection-wide variables for consistent user management
#
# All variables are defined in the collection interface (docs/source/api/variables.rst)
# and documented in meta/argument_specs.yml
# =============================================================================

# Target User Configuration
# Note: This role requires the target_user variable to be provided
# The target_user structure matches individual user objects from the collection-wide users[] variable

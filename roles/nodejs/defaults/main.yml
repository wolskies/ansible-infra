---
# =============================================================================
# NODE.JS ROLE DEFAULTS
# =============================================================================
# This role handles Node.js installation and npm package management for users
# Uses role-specific variables passed from calling roles
#
# All variables are documented in meta/argument_specs.yml
# =============================================================================

# Node.js Configuration
nodejs_version: "20"
npm_config_prefix: "~/.npm-global"
npm_config_unsafe_perm: "true"

# Package Management
node_packages: []

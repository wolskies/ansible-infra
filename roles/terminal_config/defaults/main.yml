---
# =============================================================================
# TERMINAL CONFIG ROLE DEFAULTS
# =============================================================================
# This role uses collection-wide variables from SRD section 2.2.1
# All variables defined exactly as specified in the SRD
# =============================================================================

# Terminal Configuration
terminal_entries: []

# Terminal-specific configuration mapping
terminal_configs:
  alacritty:
    terminfo_url: "https://raw.githubusercontent.com/alacritty/alacritty/master/extra/alacritty.info"
    entries: ["alacritty", "alacritty-direct"]
    tic_options: "-x"
  kitty:
    terminfo_url: "https://raw.githubusercontent.com/kovidgoyal/kitty/master/terminfo/kitty.terminfo"
    entries: ["xterm-kitty"]
    tic_options: "-x"
  wezterm:
    terminfo_url: "https://raw.githubusercontent.com/wez/wezterm/main/termwiz/data/wezterm.terminfo"
    entries: ["wezterm"]
    tic_options: "-x"

---
- name: Test os_configuration role
  hosts: all
  become: true
  tasks:
    - name: Test os_configuration role
      ansible.builtin.include_role:
        name: wolskies.infrastructure.os_configuration

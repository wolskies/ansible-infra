---
# =============================================================================
# SNAP PACKAGES ROLE VARIABLES
# =============================================================================
# Snap configuration is part of the unified infrastructure structure
# =============================================================================

infrastructure:
  Ubuntu:
    snap:
      disable_and_remove: true
      packages:
        install: []
        remove: []
        
  Debian:
    snap:
      disable_and_remove: true
      packages:
        install: []
        remove: []

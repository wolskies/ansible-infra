---
# Ansible Collection Dependencies for wolskinet.infrastructure

collections:
  - name: community.general
    version: ">=11.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.docker
    version: ">=3.4.0"
